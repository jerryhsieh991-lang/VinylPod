---
phase: 00-land-wip-reconcile
plan: 04
type: execute
wave: 4
depends_on: ["00-01", "00-02", "00-03"]
files_modified:
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/INTEGRATIONS.md
autonomous: true
requirements: [FND-04]
user_setup: []

must_haves:
  truths:
    - "No .planning/codebase/ doc still describes the WIP as sitting uncommitted in the working tree, cites the pre-WIP commit SHA as current HEAD, or calls claude.md an empty stray"
    - "CONCERNS.md's resolved items carry explicit RESOLVED (Phase 0) markers with the landing commit SHAs, so future planners read them as history, not open work"
    - "The full phase is end-to-end verified: clean tree, grouped commits present, deletions confirmed, canonical docs in place"
  artifacts:
    - "Updated .planning/codebase/CONCERNS.md (five reconciled sections)"
    - "Spot-fixed staleness in any other map doc that referenced pre-landing state"
    - "One docs(00-04) commit"
  key_links:
    - "The map was generated FROM the working tree on 2026-07-03, so file inventories already match the landed code — the staleness is concentrated in committed-vs-working-tree claims, not file lists; if deeper drift is found, flag a full /gsd-map-codebase remap instead of hand-editing everything"
---

<!-- planner-discipline-allow: f0a4c1c -->

<objective>
Refresh the `.planning/codebase/` map so it reflects committed state: mark the five now-resolved CONCERNS.md items RESOLVED with the Phase 0 commit SHAs, purge stale pre-landing references (the `f0a4c1c` pre-WIP SHA, uncommitted-WIP language, the empty-claude.md claim) from all seven map docs, and run the end-to-end phase verification. (FND-04)

Purpose: Phases 1-6 planners consume this map; if it still says the WIP is uncommitted and cites `f0a4c1c` as the last commit, every downstream plan inherits a false premise.

Output: Reconciled map docs + one commit + a recorded phase-level verification.

CRITICAL EXECUTION CONSTRAINTS:
- Execute in the MAIN working tree at /Users/jerryjerry/Desktop/VinylPodMac (not a worktree). Commit on claude/security-crash-fixes; no branch operations.
- FORBIDDEN: `git stash`, `git checkout -- <path>`, `git restore`, `git reset --hard`, `git clean`, `git add -A`, `git add .`.
- Edit map docs with scoped edits — do not regenerate/rewrite whole files by hand. If staleness is broader than the known items, note it and flag the orchestrator to re-run /gsd-map-codebase rather than hand-rewriting analysis you cannot re-derive.
</objective>

<execution_context>
@/Users/jerryjerry/.claude/gsd-core/workflows/execute-plan.md
@/Users/jerryjerry/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-land-wip-reconcile/00-01-SUMMARY.md
@.planning/phases/00-land-wip-reconcile/00-02-SUMMARY.md
@.planning/phases/00-land-wip-reconcile/00-03-SUMMARY.md
</context>

## Artifacts this phase produces

- `.planning/codebase/CONCERNS.md` — five sections reconciled with `RESOLVED (Phase 0)` markers + landing commit SHAs: "Uncommitted WIP drift" (Tech Debt), "Untracked Playwright MCP artifact dump" (Tech Debt), "Empty / stray files" (Tech Debt), "PRD vs. reality drift" (Documentation Concerns), "Documentation sprawl / overlap" (Documentation Concerns)
- Stale-reference purge across all seven map docs (pre-WIP SHA `f0a4c1c`, uncommitted/working-tree-only claims)
- Commit: `docs(00-04): refresh codebase map to committed state`
- End-to-end phase verification record in 00-04-SUMMARY.md

<tasks>

<task type="auto">
  <name>Task 1: Reconcile CONCERNS.md and purge stale pre-landing references across the map docs</name>
  <files>.planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/STACK.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (the five sections being reconciled, in full)
    - .planning/phases/00-land-wip-reconcile/00-01-SUMMARY.md (the five landing commit SHAs — cite them)
    - .planning/phases/00-land-wip-reconcile/00-02-SUMMARY.md (docs commits + the claude.md deviation record)
    - Output of `grep -rn -i -E 'f0a4c1c|uncommitted|not committed|untracked' .planning/codebase/` (the live staleness inventory)
  </read_first>
  <action>
    1. CONCERNS.md — reconcile exactly five sections. For each, prepend a bold status line `**RESOLVED (Phase 0, 2026-07-03):**` followed by a one-to-two-sentence resolution citing concrete evidence, and compress the stale body rather than leaving contradictory prose:
       - Tech Debt / "Uncommitted WIP drift": landed as five grouped commits (cite SHAs from 00-01-SUMMARY); note the real inventory was larger than the recorded 19 files (33 code paths incl. previously-untracked Capture/, Scrobbling/, Views/Settings/, MusicVisualizerContainerView.swift, e2e/); idle CPU re-profiled ~0.0% (cite the 00-01 measured mean); rename the heading to "WIP drift — landed (Phase 0)".
       - Tech Debt / "Untracked Playwright MCP artifact dump": already purged pre-phase in commit e79c990 ("chore: remove duplicate design doc and transient playwright logs"); permanently gitignored in Phase 0 (cite the 00-03 commit SHA).
       - Tech Debt / "Empty / stray files": claude.md and codex.md no longer exist anywhere — verified absent from disk, index, and untracked state during 00-02; the stray-file concern is resolved by absence (nothing was committed or deleted; cite the 00-02 reconciliation record).
       - Documentation Concerns / "PRD vs. reality drift": PRD.md now carries a HISTORICAL banner pointing to docs/system-design/ + .planning/.
       - Documentation Concerns / "Documentation sprawl / overlap": design_system 2.md removed pre-phase in e79c990 (verified in 00-02), seven feature JSONs consolidated into the 07-feature-inventory appendix, README.md removed, canonical statement added to docs/system-design/README.md; note root design_system.md remains as labeled-historical.
       Leave every OTHER concern untouched (notably "No test target anywhere" — still true, Phase 1's scope; and the bridge residual gaps — Phase 4's scope).
    2. Update the CONCERNS.md footer line to record the Phase 0 reconciliation date alongside the original audit date.
    3. Cross-doc staleness purge: from the grep inventory in read_first, replace every map-doc reference to the pre-WIP SHA `f0a4c1c` (and any "last commit" claim built on it) with the current landed reality (branch claude/security-crash-fixes, Phase 0 landing commits). The map was generated from the working tree, so file inventories should already match landed code — verify by spot-checking that ARCHITECTURE.md's component table paths (e.g. Sources/VinylPod/Views/Settings/, Scrobbling/, Capture/) all exist as tracked files via `git ls-files`. If a map doc's ANALYSIS is wrong in ways beyond these committed-state references (not just stale SHA/uncommitted language), do NOT hand-rewrite it — add a single italic note at the top of that doc that a remap via /gsd-map-codebase is recommended, and say so in the summary + final report.
    4. Commit all map-doc edits: subject `docs(00-04): refresh codebase map to committed state`.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && [ "$(grep -c 'RESOLVED (Phase 0' .planning/codebase/CONCERNS.md)" = "5" ] && [ "$(grep -rl 'f0a4c1c' .planning/codebase/ | wc -l | tr -d ' ')" = "0" ] && git log --oneline -1 | grep -q "docs(00-04)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'RESOLVED (Phase 0' .planning/codebase/CONCERNS.md` prints exactly 5
    - `grep -r 'f0a4c1c' .planning/codebase/` matches nothing
    - `grep -c 'No test target anywhere' .planning/codebase/CONCERNS.md` prints 1 (still-true concern untouched)
    - CONCERNS.md contains at least one 7-or-40-hex commit SHA from the 00-01 landing (cross-check against 00-01-SUMMARY.md)
    - `git ls-files Sources/VinylPod/Views/Settings/ Sources/VinylPod/Scrobbling/ Sources/VinylPod/Capture/ | wc -l` prints 10 (map file-inventory spot-check passes)
    - `git log --oneline -1` subject is `docs(00-04): refresh codebase map to committed state`
  </acceptance_criteria>
  <done>The map reads as a description of committed state: resolved concerns are marked with evidence, stale SHAs/claims are gone, still-open concerns are preserved, and any deeper drift is flagged for a full remap instead of hand-edited.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end phase verification against all four FND success criteria</name>
  <files>(verification only — summary records results)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 0 success criteria 1-4 — the checklist being executed)
    - .planning/phases/00-land-wip-reconcile/00-01-SUMMARY.md (CPU evidence + whether playback profiling was DEFERRED)
  </read_first>
  <action>
    Run the full phase gate and record each result in the summary:
    1. FND-01: `git status --porcelain` prints nothing; `git log --oneline -25` contains all five 00-01 subjects, three 00-02 subjects, one 00-03 subject, one 00-04 subject (additional GSD summary/metadata commits in between are expected and fine); `swift build` prints "Build complete"; 00-01-SUMMARY records idle mean < 1.0% (and playback mean < 2.0% or an explicit DEFERRED note for the end-of-phase human check).
    2. FND-02: `test -f "design_system 2.md"` fails; `ls *_features.json` at root matches nothing; `test -f README.md` fails; `grep -c 'HISTORICAL' PRD.md` >= 1; `grep -ci 'canonical' docs/system-design/README.md` >= 1; `grep -c 'Appendix A' docs/system-design/07-feature-inventory.md` >= 1; claude.md and codex.md verified ABSENT (`test ! -e claude.md && test ! -e codex.md`, and `git ls-files claude.md codex.md | wc -l` prints 0 — the stray-file criterion is satisfied by absence per the 00-02 reconciliation); docs/settings-audit.md and docs/asset-catalog-migration.md tracked (`git ls-files --error-unmatch`).
    3. FND-03: `test -d .playwright-mcp` fails; `git check-ignore -q .playwright-mcp/ VinylPod-Complete-Documentation.md` exits 0; `find . -name .DS_Store -not -path './.git/*' -not -path './.build/*'` matches nothing.
    4. FND-04: `grep -c 'RESOLVED (Phase 0' .planning/codebase/CONCERNS.md` = 5; no `f0a4c1c` anywhere under .planning/codebase/.
    5. If ANY check fails, STOP and report exactly which criterion and command failed — do not mark the phase complete. If the 00-01 playback profile was DEFERRED, state plainly in the summary and final report that the end-of-phase human check (browser playback at ~0.0% CPU in Activity Monitor) is still owed before Phase 0 is signed off.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && [ -z "$(git status --porcelain)" ] && [ "$(git log --oneline -25 | grep -c '(00-0')" -ge "10" ] && [ ! -f "design_system 2.md" ] && [ ! -f README.md ] && [ ! -e claude.md ] && [ ! -e codex.md ] && [ "$(ls *_features.json 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && grep -q HISTORICAL PRD.md && [ ! -d .playwright-mcp ] && grep -qi canonical docs/system-design/README.md</automated>
    <human-check>Confirm the widget still behaves normally after the landing: launch dist/VinylPod.app, play music in a browser tab, verify the track shows in the widget and Activity Monitor reads ~0.0% CPU at idle and during steady playback (closes the loop on any DEFERRED playback measurement from 00-01).</human-check>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` prints nothing
    - `git log --oneline -25 | grep -c '(00-0'` prints >= 10 (5+3+1+1 phase commits, plus any GSD summary/metadata commits)
    - All FND-02 checks pass: no design_system 2.md, no claude.md/codex.md (absent, per the 00-02 reconciliation), no root feature JSONs, no README.md, HISTORICAL in PRD.md, canonical in docs/system-design/README.md, Appendix A in 07-feature-inventory.md, both docs/ audits tracked
    - All FND-03 checks pass: no .playwright-mcp dir, both new ignore entries bind, zero .DS_Store files
    - All FND-04 checks pass: 5 RESOLVED markers, zero pre-WIP SHA references in the map
    - The summary records a per-criterion PASS/FAIL table and any owed human check
  </acceptance_criteria>
  <done>Every Phase 0 success criterion is machine-verified and recorded; anything owed to the end-of-phase human check is explicitly surfaced rather than silently assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map docs → future planners | Phases 1-6 trust the map as ground truth; stale claims propagate false premises into every downstream plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-12 | Tampering (analysis corruption) | Hand-edited map docs | medium | mitigate | Scoped edits only to committed-state claims; deeper drift flagged for /gsd-map-codebase remap instead of hand-rewriting derived analysis |
| T-00-13 | Repudiation (false completion) | Phase sign-off | high | mitigate | Machine-verified per-criterion gate with STOP-on-fail; DEFERRED playback measurement explicitly surfaced to the human check, never silently dropped |
</threat_model>

<verification>
1. All four FND criteria verified by the Task 2 command battery; results recorded per-criterion in the summary.
2. `git log` shows at least the ten Phase 0 work commits (5 land + 3 docs + 1 chore + 1 map) on claude/security-crash-fixes.
</verification>

<success_criteria>
- The codebase map reflects committed state (FND-04) and the whole phase is end-to-end verified with recorded evidence, including any owed human check.
</success_criteria>

<output>
Create `.planning/phases/00-land-wip-reconcile/00-04-SUMMARY.md` when done (include the per-criterion PASS/FAIL table).
</output>
