---
phase: 00-land-wip-reconcile
plan: 03
type: execute
wave: 3
depends_on: ["00-01", "00-02"]
files_modified:
  - .gitignore
  - .playwright-mcp/ (deleted, untracked transients)
  - .DS_Store, dist/.DS_Store, Sources/VinylPod/.DS_Store, SafariExtension/VinylPodConnect/.DS_Store (deleted, ignored)
autonomous: true
requirements: [FND-03]
user_setup: []

must_haves:
  truths:
    - ".playwright-mcp/ is gitignored and its ~129 transient debug artifacts are deleted"
    - "No .DS_Store file exists anywhere in the repo (outside .git/) and .DS_Store remains gitignored"
    - "git status --porcelain prints nothing — the working tree is fully clean for the first time this phase"
  artifacts:
    - ".gitignore with two new entries: .playwright-mcp/ and VinylPod-Complete-Documentation.md"
    - "One chore(00-03) commit on claude/security-crash-fixes"
  key_links:
    - "gitignore FIRST, delete SECOND — the ignore entry guarantees future artifact dumps never reappear as git noise even if the tool runs again"
    - "The clean-tree assertion here is the phase's success criterion 1 gate; it only passes because 00-01 and 00-02 dispositioned everything else first"
---

<objective>
De-noise the repo root: gitignore `.playwright-mcp/` and delete its transient debug artifacts, delete all stray `.DS_Store` files, gitignore the generated `VinylPod-Complete-Documentation.md` stitch-file, and assert the working tree is now fully clean. (FND-03)

Purpose: ~129 untracked debug logs bury real signal in `git status` and risk accidental commit of session data; the phase's "clean working tree" criterion lands here.

Output: Updated .gitignore (one commit), deleted transients, empty `git status --porcelain`.

CRITICAL EXECUTION CONSTRAINTS:
- Execute in the MAIN working tree at /Users/jerryjerry/Desktop/VinylPodMac (not a worktree). Commit on claude/security-crash-fixes; no branch operations.
- FORBIDDEN: `git stash`, `git checkout -- <path>`, `git restore`, `git reset --hard`, `git clean` (use scoped `rm` on the two named targets only), `git add -A`, `git add .`.
- The ONLY deletions permitted in this plan: the `.playwright-mcp/` directory (untracked transient debug output — deletion explicitly pre-authorized by the phase scope) and `.DS_Store` files (Finder metadata, regenerated automatically). Delete NOTHING else; VinylPod-Complete-Documentation.md is IGNORED, not deleted (it stays on disk, out of git).
- Do not touch `.claude/worktrees/` (registered git worktrees).
</objective>

<execution_context>
@/Users/jerryjerry/.claude/gsd-core/workflows/execute-plan.md
@/Users/jerryjerry/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
</context>

## Artifacts this phase produces

- `.gitignore` — appended block: comment line `# Transient debug artifacts + generated doc stitch (Phase 0)`, then `.playwright-mcp/`, then `VinylPod-Complete-Documentation.md`
- Deleted: `.playwright-mcp/` directory (~129 console-*.log / page-*.yml transients from 2026-06-29/30 debug sessions)
- Deleted: `./.DS_Store`, `dist/.DS_Store`, `Sources/VinylPod/.DS_Store`, `SafariExtension/VinylPodConnect/.DS_Store`
- Commit: `chore(00-03): gitignore playwright-mcp artifacts + generated doc stitch; purge transients`
- First fully-clean `git status` of the phase

<tasks>

<task type="auto">
  <name>Task 1: Add ignore entries for .playwright-mcp/ and the generated doc stitch, and commit</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current 11 entries — .build/, .swiftpm/, .DS_Store, xcuserdata, DerivedData/, Package.resolved, .ui_backup_*/, dist/, SafariExtension build dirs; note .DS_Store is ALREADY ignored on line 3)
    - VinylPod-Complete-Documentation.md (first 12 lines only — its own header states it is a generated stitch of the docs/system-design files "for easy download/sharing", i.e. regenerable, non-canonical)
  </read_first>
  <action>
    1. Append to .gitignore (preserving all existing entries) exactly this block:
       a comment line `# Transient debug artifacts + generated doc stitch (Phase 0)`, then the line `.playwright-mcp/`, then the line `VinylPod-Complete-Documentation.md`.
       Rationale for the second entry: the 216 KB stitch-file self-describes as a generated concatenation of the canonical docs/system-design/ set; committing it would duplicate the spec 00-02 just declared canonical, and deleting a user file is out of scope — ignoring keeps it locally without git noise.
    2. Verify both patterns bind: `git check-ignore .playwright-mcp/ VinylPod-Complete-Documentation.md` must report both paths; `git check-ignore .DS_Store` must still report a match (pre-existing line 3).
    3. Commit .gitignore alone: `git add .gitignore` then commit with subject `chore(00-03): gitignore playwright-mcp artifacts + generated doc stitch; purge transients` (the purge itself is Task 2 — the commit message covers the plan's whole effect since deletions of untracked/ignored files produce no diff to commit).
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && git check-ignore -q .playwright-mcp/ && git check-ignore -q VinylPod-Complete-Documentation.md && git check-ignore -q .DS_Store && git diff --stat HEAD -- .gitignore | grep -q "" ; git log --oneline -1 | grep -q "chore(00-03)"</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .playwright-mcp/` exits 0
    - `git check-ignore VinylPod-Complete-Documentation.md` exits 0
    - `git check-ignore .DS_Store` exits 0 (pre-existing entry intact)
    - `grep -c '^.build/$' .gitignore` prints 1 and `grep -c 'dist/' .gitignore` prints >= 1 (existing entries preserved, file appended not rewritten)
    - `git log --oneline -1` subject is `chore(00-03): gitignore playwright-mcp artifacts + generated doc stitch; purge transients`
    - `git status --porcelain` no longer lists `?? .playwright-mcp/` or `?? VinylPod-Complete-Documentation.md`
  </acceptance_criteria>
  <done>Both noise sources are ignored (so they can never re-enter git status), the ignore change is committed, and existing ignore entries are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the transient artifacts and stray .DS_Store files; assert the tree is fully clean</name>
  <files>.playwright-mcp/ (delete), 4x .DS_Store (delete)</files>
  <read_first>
    - Output of `ls .playwright-mcp/ | head` (confirm contents are exclusively console-*.log and page-*.yml transients before deleting — the pre-authorized deletion target)
  </read_first>
  <action>
    1. Safety check, then scoped deletion: confirm `ls .playwright-mcp/` shows only console-*.log and page-*.yml files (`ls .playwright-mcp/ | grep -vc -E '^(console-.*\.log|page-.*\.yml)$'` must print 0). Then `rm -rf /Users/jerryjerry/Desktop/VinylPodMac/.playwright-mcp` (absolute path, exactly this directory). If any unexpected file type is present, STOP and list it instead of deleting.
    2. Delete Finder metadata: `find /Users/jerryjerry/Desktop/VinylPodMac -name .DS_Store -not -path '*/.git/*' -not -path '*/.build/*' -delete` (known instances: repo root, dist/, Sources/VinylPod/, SafariExtension/VinylPodConnect/ — all already gitignored, regenerated automatically by Finder, zero information value).
    3. Phase-gate assertion: `git status --porcelain` must now print NOTHING. Every WIP file was committed (00-01), every doc dispositioned (00-02), every transient ignored or deleted (this plan). If any line remains, STOP and report it — an unplanned file appeared since planning and needs explicit disposition, not silent inclusion.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && [ ! -d .playwright-mcp ] && [ "$(find . -name .DS_Store -not -path './.git/*' -not -path './.build/*' | wc -l | tr -d ' ')" = "0" ] && [ -z "$(git status --porcelain)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -d .playwright-mcp` exits non-zero (directory gone)
    - `find . -name .DS_Store -not -path './.git/*' -not -path './.build/*' | wc -l` prints 0
    - `git status --porcelain` prints nothing (empty output — phase success criterion 1's clean-tree half)
    - `test -f VinylPod-Complete-Documentation.md` exits 0 (the stitch-file was ignored, NOT deleted)
    - `git log --oneline -9` still shows the five 00-01 and three 00-02 subjects (nothing was reset/lost)
  </acceptance_criteria>
  <done>Repo root is de-noised: transients purged, Finder metadata gone, generated stitch ignored in place, and the working tree is provably clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scoped rm → filesystem | The only irreversible operations this phase performs; must stay scoped to the two named targets |
| transient logs → git history | Console logs from browser debug sessions may contain browsing/session data; they must be ignored before anything else stages files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-09 | Tampering (over-deletion) | rm -rf scope | high | mitigate | Absolute-path rm of exactly .playwright-mcp; pre-deletion content check (only console-*.log/page-*.yml) with STOP on anything unexpected; find -delete restricted to the .DS_Store name with .git/.build pruned |
| T-00-10 | Information Disclosure | .playwright-mcp console logs | medium | mitigate | Ignore entry lands and is committed BEFORE deletion; logs are deleted, never staged; deletion is the pre-authorized disposal of debug session data |
| T-00-11 | Tampering (data loss) | VinylPod-Complete-Documentation.md | medium | mitigate | Ignored in place — explicitly excluded from deletion; acceptance asserts the file still exists |
</threat_model>

<verification>
1. `git check-ignore .playwright-mcp/ VinylPod-Complete-Documentation.md .DS_Store` reports all three.
2. `.playwright-mcp/` and all `.DS_Store` files are gone; the stitch-file remains on disk.
3. `git status --porcelain` is empty; `git log` retains all 00-01/00-02 commits plus one chore(00-03) commit.
</verification>

<success_criteria>
- Repo root de-noised per FND-03: transients purged, ignore entries permanent, clean working tree achieved and asserted.
</success_criteria>

<output>
Create `.planning/phases/00-land-wip-reconcile/00-03-SUMMARY.md` when done.
</output>
