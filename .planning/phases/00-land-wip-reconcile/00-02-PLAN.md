---
phase: 00-land-wip-reconcile
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - docs/system-design/07-feature-inventory.md
  - docs/system-design/README.md
  - PRD.md
  - "design_system 2.md"
  - widget_features.json
  - small_widget_features.json
  - regular_widget_features.json
  - large_widget_features.json
  - desktop_widget_features.json
  - settings_features.json
  - ui_comparative_features.json
  - claude.md
  - codex.md
  - docs/settings-audit.md
  - docs/asset-catalog-migration.md
  - README.md
autonomous: true
requirements: [FND-02]
user_setup: []

must_haves:
  truths:
    - "docs/system-design/ declares itself the single canonical spec, and no root doc claims the app is unbuilt"
    - "The seven root *_features.json are gone from the working tree and index, with their durable content folded into 07-feature-inventory.md first"
    - "design_system 2.md (the space-in-filename accidental duplicate) is deleted"
    - "claude.md and codex.md (project instruction docs) and the two untracked docs/ audits are committed; the README.md deletion is committed"
  artifacts:
    - "docs/system-design/07-feature-inventory.md with a new appendix consolidating the seven feature JSONs"
    - "docs/system-design/README.md with an explicit canonical-spec statement"
    - "PRD.md with a HISTORICAL status banner replacing the stale draft/pre-code header"
    - "Three docs commits on claude/security-crash-fixes with docs(00-02) subjects"
  key_links:
    - "Consolidation happens BEFORE `git rm` of the JSONs in the same task — content is folded in, then files are removed (git history preserves the originals)"
    - "claude.md is committed, NOT deleted — it is no longer the 0-byte stray recorded in CONCERNS.md; it now holds the 7,354-byte project instructions (planning-context deviation, documented in the summary)"
---

<objective>
Make `docs/system-design/` the single canonical spec: consolidate the seven root `*_features.json` into `07-feature-inventory.md`, delete the accidental duplicate `design_system 2.md`, replace PRD.md's stale pre-code status header, commit the project instruction docs (claude.md/codex.md) and untracked docs/ audits, and commit the WIP README.md deletion. (FND-02)

Purpose: Multiple overlapping root docs contradict each other and one claims no code exists; every future planner/executor that reads the repo must land on one truth.

Output: Updated canonical docs + three reviewable docs commits.

CRITICAL EXECUTION CONSTRAINTS:
- Execute in the MAIN working tree at /Users/jerryjerry/Desktop/VinylPodMac (not a worktree). Commit on the current branch claude/security-crash-fixes; no branch operations.
- FORBIDDEN: `git stash`, `git checkout -- <path>`, `git restore`, `git reset --hard`, `git clean`, `git add -A`, `git add .`.
- Deletions in this plan are ONLY of git-tracked files via `git rm` (recoverable from history). Never `rm` an untracked file in this plan.
- KNOWN DEVIATION from the phase pre-scope: the success criterion "the empty root claude.md is removed" is stale — claude.md is now 7,354 bytes of project instructions ("# Claude Instructions for VinylPod", written 2026-07-03). Deleting it would destroy the user's only copy (forbidden). The criterion's intent — no confusing no-op instruction file — is satisfied by COMMITTING the populated file. Record this deviation in the summary.
</objective>

<execution_context>
@/Users/jerryjerry/.claude/gsd-core/workflows/execute-plan.md
@/Users/jerryjerry/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@docs/system-design/README.md
</context>

## Artifacts this phase produces

- `docs/system-design/07-feature-inventory.md` — new final section `## Appendix A — Historical build-spec digests (consolidated from root *_features.json, 2026-07-03)` preserving durable facts from the seven JSONs (target sizes, accent hexes, layout geometry, hotkey-scope decision, done-flags summary)
- `docs/system-design/README.md` — new canonical-spec statement paragraph
- `PRD.md` — HISTORICAL status banner (draft/pre-code header replaced; points to docs/system-design/ + .planning/ as current truth)
- Deleted (via `git rm`, preserved in history): `design_system 2.md`, `widget_features.json`, `small_widget_features.json`, `regular_widget_features.json`, `large_widget_features.json`, `desktop_widget_features.json`, `settings_features.json`, `ui_comparative_features.json`
- Committed: `claude.md`, `codex.md`, `docs/settings-audit.md`, `docs/asset-catalog-migration.md`, and the `README.md` deletion
- Commits: `docs(00-02): consolidate root feature JSONs into 07-feature-inventory appendix`, `docs(00-02): designate docs/system-design canonical; mark PRD historical; drop design_system duplicate`, `docs(00-02): land project instruction docs and remove superseded README`

<tasks>

<task type="auto">
  <name>Task 1: Consolidate the seven root feature JSONs into 07-feature-inventory.md, then git rm them</name>
  <files>docs/system-design/07-feature-inventory.md, widget_features.json, small_widget_features.json, regular_widget_features.json, large_widget_features.json, desktop_widget_features.json, settings_features.json, ui_comparative_features.json</files>
  <read_first>
    - docs/system-design/07-feature-inventory.md (the full current census — know what is ALREADY covered before folding)
    - widget_features.json, small_widget_features.json, regular_widget_features.json, large_widget_features.json, desktop_widget_features.json, settings_features.json, ui_comparative_features.json (all seven, in full — they are 1.3-2.6 KB each)
  </read_first>
  <action>
    The seven JSONs are June-28 screenshot-rebuild task specs (schema_version 1/2, "done" flags, layout geometry) that predate the deeper 07-feature-inventory.md census. Consolidate info-preservingly:
    1. For each JSON, extract the durable facts not already present in 07-feature-inventory.md or other docs/system-design files. At minimum preserve: per-size target dimensions (small 162x162, regular 300x360 default, large 320x432, desktop full-screen), the per-size primary accent hexes (#C592AB, #D18FB8, #BD6BAD), notable layout geometry (artwork sizes/insets, control placements), the settings_features.json decisions block (hotkeyScope: global Carbon RegisterEventHotKey, no Accessibility permission) and its node->file mapping, and each file's one-line description with its done-status summary.
    2. Append a new final section to docs/system-design/07-feature-inventory.md titled exactly `## Appendix A — Historical build-spec digests (consolidated from root *_features.json, 2026-07-03)`. One subsection per JSON (use the original filename as the subsection heading) containing a compact digest of the extracted facts. State at the top of the appendix that the original JSONs were removed in Phase 0 and remain retrievable from git history.
    3. Only after the appendix is written and saved: `git rm widget_features.json small_widget_features.json regular_widget_features.json large_widget_features.json desktop_widget_features.json settings_features.json ui_comparative_features.json`.
    4. Do NOT touch BrowserExtension/extension_backend_features.json — it is not one of the seven root files and stays where it is.
    5. Commit: subject `docs(00-02): consolidate root feature JSONs into 07-feature-inventory appendix`, staging exactly the seven removals + docs/system-design/07-feature-inventory.md.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && grep -q "Appendix A — Historical build-spec digests" docs/system-design/07-feature-inventory.md && [ "$(ls *_features.json 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && [ "$(git ls-files '*_features.json')" = "BrowserExtension/extension_backend_features.json" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Appendix A — Historical build-spec digests' docs/system-design/07-feature-inventory.md` prints 1
    - The appendix contains seven subsections, one per original filename (grep finds `widget_features.json`, `settings_features.json`, `ui_comparative_features.json` etc. inside 07-feature-inventory.md)
    - The appendix preserves the target dimensions (grep finds `162`, `300 x 360` or `300x360`, `320 x 432` or `320x432`) and the accent hexes (grep finds `#C592AB`, `#D18FB8`, `#BD6BAD`)
    - `ls *_features.json` at repo root matches nothing (exit non-zero / count 0)
    - `git ls-files '*_features.json'` prints only BrowserExtension/extension_backend_features.json
    - `git log --oneline -1` subject is `docs(00-02): consolidate root feature JSONs into 07-feature-inventory appendix`
  </acceptance_criteria>
  <done>All durable content from the seven JSONs lives in the 07-feature-inventory appendix; the seven files are removed from tree and index via git rm; history preserves the originals.</done>
</task>

<task type="auto">
  <name>Task 2: Designate docs/system-design canonical, replace PRD.md's stale status header, delete design_system 2.md</name>
  <files>docs/system-design/README.md, PRD.md, "design_system 2.md"</files>
  <read_first>
    - PRD.md (the full header block — the three stale blockquote lines to replace, and the body that stays)
    - docs/system-design/README.md (intro paragraphs — where the canonical statement slots in)
    - CONTRACTS.md (stays authoritative for frozen API names — the canonical statement must NOT demote it)
  </read_first>
  <action>
    1. PRD.md: replace the stale header blockquote (the three lines asserting a draft awaiting sign-off, a thinking/planning phase with zero code, dated 2026-06-28) with a historical banner blockquote stating: Status: HISTORICAL — founding PRD, kept for original intent; the app is built (~9,300 LOC Swift + MV3 extension); current truth lives in `docs/system-design/` (canonical spec) and `.planning/` (requirements/roadmap); original draft date 2026-06-28. Change nothing below the header — the body is preserved founding intent. The result must contain no claim that code has not been written.
    2. docs/system-design/README.md: after the opening intro paragraph, insert a short paragraph titled/bolded as canonical notice stating: this folder is the single canonical product + architecture spec; root-level `PRD.md` and `design_system.md` are historical; `CONTRACTS.md` remains the frozen public-API name contract; `.planning/` holds current requirements and roadmap.
    3. Delete the accidental duplicate: `git rm "design_system 2.md"` (note the space in the filename — quote it). Root `design_system.md` (22 KB) STAYS — the phase criteria only mandate removing the " 2" duplicate; it is now labeled historical by the README statement.
    4. Commit: subject `docs(00-02): designate docs/system-design canonical; mark PRD historical; drop design_system duplicate`, staging exactly PRD.md, docs/system-design/README.md, and the design_system 2.md removal.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && grep -qi "canonical" docs/system-design/README.md && grep -q "HISTORICAL" PRD.md && ! grep -q "no application code written yet" PRD.md && ! grep -q "awaiting founder sign-off" PRD.md && [ ! -f "design_system 2.md" ] && [ -f design_system.md ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'canonical' docs/system-design/README.md` prints >= 1
    - `grep -c 'HISTORICAL' PRD.md` prints >= 1
    - `grep -c 'no application code written yet' PRD.md` prints 0
    - `grep -c 'awaiting founder sign-off' PRD.md` prints 0
    - `grep -c 'docs/system-design' PRD.md` prints >= 1 (the banner points to the canonical spec)
    - `test -f "design_system 2.md"` exits non-zero AND `git ls-files "design_system 2.md" | wc -l` prints 0
    - `test -f design_system.md` exits 0 (the non-duplicate stays)
    - `grep -c 'CONTRACTS.md' docs/system-design/README.md` prints >= 1 (frozen contract not demoted)
  </acceptance_criteria>
  <done>No doc claims the app is unbuilt; docs/system-design/ carries an explicit canonical statement that keeps CONTRACTS.md authoritative; the space-in-filename duplicate is gone from tree and index.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the project instruction docs, the docs/ audits, and the README.md deletion</name>
  <files>claude.md, codex.md, docs/settings-audit.md, docs/asset-catalog-migration.md, README.md</files>
  <read_first>
    - claude.md (confirm it is the populated 7,354-byte "Claude Instructions for VinylPod" project brain, NOT a 0-byte stray — this is the deviation gate)
    - codex.md (companion instruction doc for Codex)
    - docs/settings-audit.md and docs/asset-catalog-migration.md (untracked audits being landed)
  </read_first>
  <action>
    1. Deviation gate: verify claude.md is non-empty and starts with the heading `# Claude Instructions for VinylPod`. If it is 0 bytes, the planning-context assumption held after all — in that case `rm claude.md` (empty file, nothing lost) and skip committing it. Otherwise (expected): COMMIT it — it is the project instruction file; document in the summary that FND-02's "empty claude.md removed" was satisfied by committing the now-populated file instead.
    2. Stage exactly: `git add claude.md codex.md docs/settings-audit.md docs/asset-catalog-migration.md README.md` (adding README.md stages its WIP deletion — README content remains in git history; its build-toolchain notes are superseded by docs/system-design/05-security-performance-build.md).
    3. Commit: subject `docs(00-02): land project instruction docs and remove superseded README`. Body: note claude.md/codex.md are the Claude/Codex-facing project brains, and README.md removal is part of the docs consolidation (history preserves it).
    4. Do NOT add VinylPod-Complete-Documentation.md (216 KB generated stitch-file of docs/system-design — plan 00-03 gitignores it) and do NOT touch .playwright-mcp/.
  </action>
  <verify>
    <automated>cd /Users/jerryjerry/Desktop/VinylPodMac && git ls-files --error-unmatch claude.md codex.md docs/settings-audit.md docs/asset-catalog-migration.md >/dev/null 2>&1 && [ "$(git ls-files README.md | wc -l | tr -d ' ')" = "0" ] && ! git status --porcelain | grep -E "^(\?\?| D|D ) (claude.md|codex.md|README.md|docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch claude.md codex.md docs/settings-audit.md docs/asset-catalog-migration.md` exits 0 (all four tracked)
    - `head -1 claude.md` prints `# Claude Instructions for VinylPod` (committed populated, not empty)
    - `git ls-files README.md | wc -l` prints 0 and `test -f README.md` exits non-zero (deletion committed)
    - `git log --oneline -1` subject is `docs(00-02): land project instruction docs and remove superseded README`
    - `git status --porcelain` no longer lists claude.md, codex.md, README.md, or any docs/ path; remaining entries are only `?? .playwright-mcp/` and `?? VinylPod-Complete-Documentation.md` (plan 00-03's scope)
  </acceptance_criteria>
  <done>Instruction docs and audits are tracked, the README deletion is landed, and the only remaining working-tree noise is exactly what plan 00-03 dispositions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked docs → git history | Deletions must be `git rm` on tracked files only, so every removed byte stays recoverable |
| untracked docs (only copy) → repo | claude.md/codex.md/audits exist nowhere else; deletion instead of commit would be unrecoverable loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-00-05 | Tampering (data loss) | claude.md / codex.md / docs audits | critical | mitigate | Deviation gate: verify claude.md is the populated instruction file and COMMIT it; `rm` allowed only if it is genuinely 0 bytes; untracked files are never rm'd otherwise |
| T-00-06 | Repudiation (lost provenance) | Seven feature JSONs | medium | mitigate | Appendix digest written and committed BEFORE `git rm`; removal commit records that originals live in history |
| T-00-07 | Tampering (over-deletion) | `design_system 2.md` vs `design_system.md` | medium | mitigate | Quoted exact filename in `git rm "design_system 2.md"`; acceptance asserts design_system.md still exists |
| T-00-08 | Information Disclosure | Accidental staging of transient logs | high | mitigate | Explicit `git add` path lists only; acceptance asserts `.playwright-mcp/` still untracked after every commit |
</threat_model>

<verification>
1. `git log --oneline -3` shows the three docs(00-02) subjects.
2. Root has no `*_features.json`, no `design_system 2.md`, no README.md; PRD.md carries the HISTORICAL banner; docs/system-design/README.md carries the canonical statement.
3. `git status --porcelain` lists only `?? .playwright-mcp/` and `?? VinylPod-Complete-Documentation.md`.
</verification>

<success_criteria>
- docs/system-design/ is explicitly canonical; no doc claims the app is unbuilt.
- Feature-JSON content is preserved in the 07 appendix before removal; all deletions are history-recoverable.
- The claude.md deviation (populated, committed instead of deleted) is documented in the summary.
</success_criteria>

<output>
Create `.planning/phases/00-land-wip-reconcile/00-02-SUMMARY.md` when done (explicitly record the claude.md deviation and the three commit SHAs).
</output>
